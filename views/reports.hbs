<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reports</title>
  
</head>
<body>
<a href="/home" style="background-color:bisque; color:blueviolet; padding: 5px 10px; border-radius: 5px; text-decoration: none; cursor: pointer; display:inline-flex; margin-bottom: 20px;">Home</a>
<a href="http://localhost:5000/search.html" style="background-color:bisque; color:blueviolet; padding: 5px 10px; border-radius: 5px; text-decoration: none; cursor: pointer; display:inline-flex; margin-bottom: 20px;">search</a>
  <h1>Reports</h1>

 <table>
    <thead>
      <tr>
        <th>Author</th>
        <th>Title</th>
        <th>Year</th>
        <th>Keywords</th>
        <th>File_ID</th>
        <th>Edit</th>
        <th>Delete</th>
      </tr>
    </thead>
    <tbody>
      {{#each reports}}
        <tr>
          <td>{{author}}</td>
          <td>{{file_title}}</td>
          <td>{{year}}</td>
          <td>{{keywords}}</td>
          <td>{{file_id}}</td>
          <td>
            <a href="/reports/edit/{{encodeURIComponent author}}" class="edit-button">Edit</a>
          </td>
          <td>
            <form action="/reports/delete/{{author}}" method="POST" class="delete-form" onsubmit="return confirm('Are you sure you want to delete this report?')">
              <input type="hidden" name="author" value="{{author}}">
              <input type="submit" value="Delete" class="delete-button">
            </form>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>


</body>

<style>
table {
  font-family:'Times New Roman', Times, serif;
  border-collapse: collapse;
  width: 100%;
  font-size:medium;
}

td, th {
  border: 1px solid #dddddd;
  text-align: left;
  padding: 8px;
 
}

{{!-- tr:nth-child(even) {
  background-color: #dddddd;
} --}}

.delete-button {
  background-color: red;
  color: white;
  padding: 5px 10px;
  border-radius: 5px;
  text-decoration: none;
  cursor: pointer;
}

.edit-button {
  background-color: green;
  color: white;
  padding: 5px 10px;
  border-radius: 5px;
  text-decoration: none;
  cursor: pointer;
  margin-right: 10px;
}
</style>


</html>
